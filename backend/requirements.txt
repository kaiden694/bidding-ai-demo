# ===== 智能招投标与合同合规 AI 工作台 — 后端依赖 =====
# Python 3.11+

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# 数据层
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# 缓存/任务队列
redis==5.2.1
celery[redis]==5.4.0

# 数据校验
pydantic==2.10.4
pydantic-settings==2.7.0

# 权限/认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
casbin==1.34.0
casbin-sqlalchemy-adapter==1.4.0

# 对象存储
minio==7.2.13

# 文档解析
PyMuPDF==1.25.2
pdfplumber==0.11.4
python-docx==1.1.2
# unstructured / rapidocr-onnxruntime 未在代码中使用（可选导入），暂不安装以减小镜像体积

# AI 引擎（仅使用 OpenAI 兼容接口调用远程 LLM/Embedding，无需本地推理）
openai==1.59.6

# HTTP/重试/限流
httpx==0.28.1
tenacity==9.0.0
slowapi==0.1.9

# 工具
loguru==0.7.3
python-multipart==0.0.20
orjson==3.10.13
dateutils==0.6.12
transitions==0.9.2
python-docx==1.1.2

# 监控
prometheus-client==0.21.1

# 可观测性 — OpenTelemetry 链路追踪（可选，OTEL_ENABLED=false 时降级运行）
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-instrumentation-redis==0.48b0

# 测试
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
